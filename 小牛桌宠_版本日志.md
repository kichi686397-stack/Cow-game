# 小牛桌宠版本日志

## v10.3.9 - 排行榜窗口整理

- 排行榜弹窗改为固定尺寸，避免列表内容变化时撑高窗口。
- 排行榜分类按钮、滚动列表、底部操作按钮拆成固定区域，滚动条收进内容列表内部。
- 更新日志弹窗同步改为固定窗口加内部滚动区域，避免整窗滚动影响底部关闭按钮。

## v10.3.8 - 入口逻辑修复

- 修复 `#workBtn` 同时触发新版耕地小游戏和旧版主页面打工进度条的问题。
- 保留新版 `openFarmGame()` 入口，移除旧版 `#workBtn.addEventListener('click', ...)` 打工计时逻辑。
- 测试面板 `t-fish` 改为调用 `openFishGameOverlay()`，与普通菜单钓鱼入口保持一致。
- 新手教程钓鱼文案同步新版玩法：按住蓄力、松开下钩、瞄准鱼群。
- `fruit_game.html`、`farm_game.html`、`fishing_game.html` 补充 data favicon，减少直接访问时的 404 噪声。

## v10.3.7 - 版本配置拆分

- 新增 `cowgame_deploy/shared/version-config.js`。
- 将 `CURRENT_VER` 从 `cowgame_deploy/index.html` 迁移到共享版本配置。
- 将新版本弹窗配置从主文件迁移到共享版本配置。
- 历史 `CHANGELOG` 渲染结构暂时保留在主文件内，后续可继续迁移为独立日志数据。

## v10.3.6 - 共享配置抽取

- 新增 `cowgame_deploy/shared/game-config.js`。
- 将宇宙钓鱼物品池从 `cowgame_deploy/index.html` 迁移到共享配置。
- 将排行榜分类、单位、排序方向和说明文案迁移到共享配置。
- 主文件通过 `window.COWGAME_GAME_CONFIG` 读取共享游戏配置。

## v10.3.5 - 接水果入口整理

- 接水果小游戏统一保留 `fruit_game.html` iframe 版本。
- 移除 `cowgame_deploy/index.html` 内旧的 `gameOverlay` / `gameWrap` 接水果弹层。
- 移除主文件内旧的接水果运行状态、掉落逻辑、连击逻辑和重复水果池。
- 保留主游戏接收 `fruitGameComplete` 消息后的 XP、成就、存档和排行榜上传流程。

## v10.3.4 - 排行榜路线整理

- 明确当前排行榜继续使用 Supabase REST 接口。
- 移除未被前端调用的 Cloudflare KV leaderboard worker 草稿。
- 清理拼写错误的 `fucntions` 目录，避免误以为 Cloudflare Pages Functions 已接入。
- 保留现有排行榜数据流：主页面直接通过 Supabase 接口读取与上传排行榜。

## v10.3.3 - 开发规范校准

- 明确当前主文件为 `cowgame_deploy/index.html`。
- 明确版本日志文件为 `小牛桌宠_版本日志.md`。
- 每次改动需要同步更新页面 `<title>` 版本号、`CURRENT_VER`、游戏内 `CHANGELOG` 和本文件。
- 版本号遵循 SemVer：
  - Major：新增独立核心系统 / 架构级重构
  - Minor：向下兼容的新功能
  - Patch：Bug 修复 / 小改动 / 文档与维护规则调整
- 后续小牛与访客动画优先使用图片帧 / 精灵帧，不再新增整头牛 Canvas 手绘逻辑。
- Canvas 主要保留给现有兼容层、场景特效、小游戏画布和过渡期迁移。
- 存档继续使用 `localStorage`，key = `pixelcow_save_v1`。

## v10.3.2 - 互动优化 + 动物表现升级

- 修复小牛升级后小鸟 / 蝴蝶落点错误。
- 调整小猫移动节奏。
- 新增小猫互动动作。
- 修复蝴蝶体型偏大问题。
- 扩展成就系统与装扮内容。
- 存档字段扩展，保存青蛙与小猫互动统计。
